# README

This is my personal neovim config; initally bootstrapped from https://github.com/nvim-lua/kickstart.nvim

## Essentials
This config sets the `<leader>` to a single whitespace by default.

- `<leader>e` - Toggle nvim-tree (file [e]xplorer)
- `<leader>sf` - [s]earch for [f]iles by name 

